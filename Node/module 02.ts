[
  {
    "id": 61,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "ACID Properties",
    "question": "What does the acronym ACID stand for in the context of relational database transactions?",
    "code_snippet": null,
    "options": ["Automated, Cached, Indexed, Distributed", "Atomicity, Consistency, Isolation, Durability", "Asynchronous, Concurrent, Isolated, Durable", "Authenticated, Encrypted, Integrated, Deployed"],
    "correct_answer": "Atomicity, Consistency, Isolation, Durability",
    "explanation": "ACID is the holy grail of relational databases. Atomicity ensures all-or-nothing execution. Consistency ensures data follows rules (constraints). Isolation ensures concurrent transactions don't interfere. Durability guarantees saved data survives crashes."
  },
  {
    "id": 62,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "MVCC (Multi-Version Concurrency Control)",
    "question": "How does PostgreSQL handle concurrent reads and writes without locking the entire table?",
    "code_snippet": null,
    "options": ["It uses a single-threaded queue for all queries", "It uses MVCC, where every update creates a new 'version' of the row, allowing readers to see the old version while writers create the new one", "It blocks all reads until the write is finished", "It copies the entire database to memory"],
    "correct_answer": "It uses MVCC, where every update creates a new 'version' of the row, allowing readers to see the old version while writers create the new one",
    "explanation": "MVCC is PostgreSQL's superpower. Readers never block writers, and writers never block readers. The database maintains multiple versions of a row, and the vacuum process later cleans up the old, invisible versions."
  },
  {
    "id": 63,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "B-Tree Indexes",
    "question": "What is the default index type in PostgreSQL, and what kind of queries is it best suited for?",
    "code_snippet": null,
    "options": ["Hash Index; exact match only", "B-Tree; equality (`=`) and range queries (`<`, `>`, `BETWEEN`, `ORDER BY`)", "GIN; full-text search", "GiST; geometric data"],
    "correct_answer": "B-Tree; equality (`=`) and range queries (`<`, `>`, `BETWEEN`, `ORDER BY`)",
    "explanation": "The B-Tree (Balanced Tree) is the standard index. It organizes data in a sorted tree structure, making it incredibly fast for finding exact values, finding ranges of values, and sorting data without a separate 'filesort' step."
  },
  {
    "id": 64,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Composite Indexes (Left-Most Prefix)",
    "question": "If you create a composite index on `(last_name, first_name)`, which of the following queries can utilize the index?",
    "code_snippet": null,
    "options": ["Only queries filtering by `first_name`", "Queries filtering by `last_name` alone, or `last_name` AND `first_name`", "Queries filtering by `first_name` alone", "All queries regardless of column order"],
    "correct_answer": "Queries filtering by `last_name` alone, or `last_name` AND `first_name`",
    "explanation": "Composite indexes follow the 'left-most prefix' rule. Think of it like a phone book: it's sorted by last name first. You can search by last name, or last name + first name. But you cannot efficiently search by first name alone without scanning the whole index."
  },
  {
    "id": 65,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "GIN Indexes",
    "question": "What is a GIN (Generalized Inverted Index) used for in PostgreSQL?",
    "code_snippet": null,
    "options": ["Sorting numeric columns", "Indexing composite types like arrays, JSONB, and full-text search vectors", "Enforcing foreign key constraints", "Encrypting sensitive columns"],
    "correct_answer": "Indexing composite types like arrays, JSONB, and full-text search vectors",
    "explanation": "If you have a `JSONB` column or an array of tags, a standard B-Tree can't index the internal keys/values. A GIN index breaks the composite data apart and creates an inverted index (like the index at the back of a book), making searches inside JSON/arrays lightning fast."
  },
  {
    "id": 66,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Query Planning (EXPLAIN ANALYZE)",
    "question": "What is the difference between `EXPLAIN` and `EXPLAIN ANALYZE` in SQL?",
    "code_snippet": null,
    "options": ["They are the same", "`EXPLAIN` shows the database's estimated execution plan; `EXPLAIN ANALYZE` actually executes the query and shows the real-time execution statistics and bottlenecks", "`EXPLAIN ANALYZE` is for deleting tables", "`EXPLAIN` is deprecated"],
    "correct_answer": "`EXPLAIN` shows the database's estimated execution plan; `EXPLAIN ANALYZE` shows the real-time execution statistics and bottlenecks",
    "explanation": "Always use `EXPLAIN ANALYZE` when debugging slow queries in development. It will tell you exactly if the database is doing a 'Sequential Scan' (reading the whole table) instead of an 'Index Scan', and exactly how many milliseconds each step took."
  },
  {
    "id": 67,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Connection Pooling (Concept)",
    "question": "Why is 'Connection Pooling' absolutely critical for a Node.js backend connecting to PostgreSQL?",
    "code_snippet": null,
    "options": ["It caches the SQL queries", "Establishing a new TCP connection and performing the PostgreSQL handshake is expensive; pooling reuses existing connections, saving massive CPU and latency", "It encrypts the data", "It automatically writes migrations"],
    "correct_answer": "Establishing a new TCP connection and performing the PostgreSQL handshake is expensive; pooling reuses existing connections, saving massive CPU and latency",
    "explanation": "If you have 1,000 concurrent users and create a new DB connection for each, PostgreSQL will crash from memory exhaustion. A pool (like `pg-pool`) keeps a set number of connections open and hands them out to your Node.js requests as needed."
  },
  {
    "id": 68,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Connection Pool Exhaustion",
    "question": "What happens when your Node.js connection pool is fully exhausted (all connections are busy)?",
    "code_snippet": null,
    "options": ["The server crashes immediately", "New database queries are queued in Node.js memory until a connection is freed, or they time out and throw an error if they wait too long", "PostgreSQL automatically creates more connections", "The queries are dropped silently"],
    "correct_answer": "New database queries are queued in Node.js memory until a connection is freed, or they time out and throw an error if they wait too long",
    "explanation": "Pool exhaustion is a silent killer. Your Node.js app won't crash, but requests will hang indefinitely waiting for a DB connection, eventually causing API timeouts and a terrible user experience. Monitoring pool wait times is crucial."
  },
  {
    "id": 69,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Transactions (BEGIN/COMMIT)",
    "question": "In a Node.js ORM or raw SQL client, how do you ensure that two separate `UPDATE` queries either both succeed or both fail?",
    "code_snippet": null,
    "options": ["Run them in parallel", "Wrap them in a Transaction block (`BEGIN`, then `COMMIT` if both succeed, or `ROLLBACK` if either fails)", "Use `Promise.all()`", "Add a `try/catch` block around them"],
    "correct_answer": "Wrap them in a Transaction block (`BEGIN`, then `COMMIT` if both succeed, or `ROLLBACK` if either fails)",
    "explanation": "A `try/catch` in Node.js only catches JS errors. If the second SQL query fails, the first one has already modified the database. A transaction tells the DB to hold all changes in a temporary state until you explicitly say `COMMIT`."
  },
  {
    "id": 70,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Isolation Levels (Dirty Reads)",
    "question": "What is a 'Dirty Read' in database concurrency?",
    "code_snippet": null,
    "options": ["Reading data from a corrupted disk", "When a transaction reads uncommitted data written by another concurrent transaction that might later be rolled back", "Reading data without an index", "Reading data from a different database"],
    "correct_answer": "When a transaction reads uncommitted data written by another concurrent transaction that might later be rolled back",
    "explanation": "If Transaction A updates a row, and Transaction B reads it before A commits, B is reading 'dirty' data. If A then rolls back, B has based its logic on data that technically never existed. PostgreSQL's default 'Read Committed' level prevents dirty reads."
  },
  {
    "id": 71,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Isolation Levels (Serializable)",
    "question": "What is the highest isolation level in SQL, and what is its main drawback?",
    "code_snippet": null,
    "options": ["Read Uncommitted; it's too slow", "Serializable; it prevents all anomalies (like write skew and phantom reads) by making transactions act as if they ran sequentially, but it heavily increases the chance of serialization failures and rollbacks", "Repeatable Read; it locks the whole table", "None, SQL is always perfectly isolated"],
    "correct_answer": "Serializable; it prevents all anomalies by making transactions act as if they ran sequentially, but it heavily increases the chance of serialization failures and rollbacks",
    "explanation": "Serializable is the safest but most restrictive. If two transactions try to modify related data concurrently, the DB will forcefully abort one of them with a serialization error, requiring your Node.js app to catch the error and retry the transaction."
  },
  {
    "id": 72,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Deadlocks",
    "question": "What is a 'Deadlock' in a relational database?",
    "code_snippet": null,
    "options": ["When the database runs out of disk space", "When two transactions are waiting on each other to release locks, creating an infinite cycle that the database must detect and resolve by killing one transaction", "When a connection pool is full", "When an index is corrupted"],
    "correct_answer": "When two transactions are waiting on each other to release locks, creating an infinite cycle that the database must detect and resolve by killing one transaction",
    "explanation": "Transaction A locks Row 1 and wants Row 2. Transaction B locks Row 2 and wants Row 1. Neither can proceed. The DB's deadlock detector will notice this, pick a 'victim' transaction, roll it back, and let the other one finish."
  },
  {
    "id": 73,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "The N+1 Query Problem",
    "question": "What is the 'N+1 Query Problem' in the context of ORMs and database performance?",
    "code_snippet": null,
    "options": ["When a query takes N+1 seconds to run", "When fetching a list of N parent records triggers N additional queries to fetch their related child records, resulting in N+1 total queries instead of 1", "When the database has N+1 tables", "When an index has N+1 columns"],
    "correct_answer": "When fetching a list of N parent records triggers N additional queries to fetch their related child records, resulting in N+1 total queries instead of 1",
    "explanation": "If you fetch 100 `Users`, and then loop through them to get their `Posts`, an ORM might execute 1 query for the users, and 100 queries for the posts. This destroys database performance. The fix is to use `JOIN`s or eager loading."
  },
  {
    "id": 74,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Solving N+1 (DataLoader / JOINs)",
    "question": "What are the two primary ways to solve the N+1 Query Problem?",
    "code_snippet": null,
    "options": ["Increase the connection pool size", "Use SQL `JOIN`s to fetch everything in one query, or use a batching utility like DataLoader to group the child queries into a single `WHERE id IN (...)` query", "Use `Promise.all()`", "Disable transactions"],
    "correct_answer": "Use SQL `JOIN`s to fetch everything in one query, or use a batching utility like DataLoader to group the child queries into a single `WHERE id IN (...)` query",
    "explanation": "SQL `JOIN`s are the traditional fix. However, in GraphQL or complex ORMs, `DataLoader` is used to intercept the N individual requests, batch them into a single array of IDs, and execute exactly ONE query to the database."
  },
  {
    "id": 75,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Database Migrations",
    "question": "What is a 'Database Migration' in a modern TypeScript backend?",
    "code_snippet": null,
    "options": ["Moving the database to a new server", "A version-controlled script that incrementally applies (or rolls back) schema changes (like adding a column) to the database in a safe, repeatable way", "Backing up the database", "Converting SQL to NoSQL"],
    "correct_answer": "A version-controlled script that incrementally applies (or rolls back) schema changes to the database in a safe, repeatable way",
    "explanation": "Just like Git tracks code changes, migration tools (like Prisma Migrate or Drizzle Kit) track SQL schema changes. This ensures that your local dev DB, staging DB, and production DB can all be perfectly synchronized using code."
  },
  {
    "id": 76,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Prisma (Schema-First)",
    "question": "What is the core philosophy of Prisma ORM?",
    "code_snippet": null,
    "options": ["Writing raw SQL strings in TypeScript", "Schema-First: You define your database schema in a custom `schema.prisma` file, and Prisma generates the SQL migrations and the fully type-safe TypeScript client", "Writing TypeScript classes that map to tables", "Using a visual drag-and-drop builder"],
    "correct_answer": "Schema-First: You define your database schema in a custom `schema.prisma` file, and Prisma generates the SQL migrations and the fully type-safe TypeScript client",
    "explanation": "Prisma abstracts away raw SQL. You write a declarative schema, run `prisma migrate`, and it generates a `PrismaClient` where every query and return type is perfectly typed based on your schema."
  },
  {
    "id": 77,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Drizzle ORM (Code-First)",
    "question": "How does Drizzle ORM differ from Prisma in its approach to TypeScript?",
    "code_snippet": null,
    "options": ["Drizzle doesn't support TypeScript", "Drizzle is Code-First: You define your schema using TypeScript functions/objects, and Drizzle infers the types and pushes the SQL schema to the database", "Drizzle requires a separate schema file", "Drizzle is only for NoSQL"],
    "correct_answer": "Drizzle is Code-First: You define your schema using TypeScript functions/objects, and Drizzle infers the types and pushes the SQL schema to the database",
    "explanation": "Drizzle keeps you closer to SQL. Your schema is just TypeScript code. This means your database schema and your application code live in the exact same file, and the type inference is incredibly fast and lightweight."
  },
  {
    "id": 78,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Query Builders (Kysely)",
    "question": "What is a 'Query Builder' like Kysely, and why do some developers prefer it over a full ORM?",
    "code_snippet": null,
    "options": ["It builds the database server", "It provides a type-safe, fluent API to write SQL queries using JavaScript objects, without hiding the underlying SQL or managing object-relational mapping", "It automatically writes unit tests", "It converts SQL to GraphQL"],
    "correct_answer": "It provides a type-safe, fluent API to write SQL queries using JavaScript objects, without hiding the underlying SQL or managing object-relational mapping",
    "explanation": "Full ORMs (like Prisma) can generate slow, bloated SQL. Query Builders (like Kysely) give you 100% of the power and performance of raw SQL, but with full TypeScript autocomplete and type-safety."
  },
  {
    "id": 79,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Normalization (3NF)",
    "question": "What is the goal of 'Third Normal Form' (3NF) in relational database design?",
    "code_snippet": null,
    "options": ["To make queries run faster", "To eliminate redundant data and ensure that every non-key column is dependent only on the primary key, preventing update anomalies", "To encrypt the database", "To convert the database to JSON"],
    "correct_answer": "To eliminate redundant data and ensure that every non-key column is dependent only on the primary key, preventing update anomalies",
    "explanation": "If you store a user's `country_name` in the `users` table instead of a `country_id`, and the country changes its name, you have to update thousands of rows. 3NF forces you to use foreign keys, ensuring data integrity."
  },
  {
    "id": 80,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Part 1 Wrap-up",
    "question": "You've now covered PostgreSQL internals, indexing, transactions, and modern TypeScript ORMs. What is the ultimate takeaway for a backend engineer mastering the database layer?",
    "code_snippet": null,
    "options": ["To memorize every SQL command", "To understand that the database is the heart of the application; writing efficient queries, managing connections, and ensuring data integrity is what separates a fragile app from an enterprise-grade system", "To avoid using indexes", "To always use NoSQL"],
    "correct_answer": "To understand that the database is the heart of the application; writing efficient queries, managing connections, and ensuring data integrity is what separates a fragile app from an enterprise-grade system",
    "explanation": "Your frontend can be perfect, but if your database queries are unindexed, your transactions aren't isolated, or your connection pool is leaking, your app will fail under pressure. Mastering the DB is mastering the backend."
  }
]
[
  {
    "id": 81,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Window Functions",
    "question": "What is a 'Window Function' in SQL, and how does it differ from a standard `GROUP BY` aggregate?",
    "code_snippet": null,
    "options": ["It groups rows into a single output row", "It performs calculations across a set of rows related to the current row, but WITHOUT collapsing them into a single output row (preserving the original row identity)", "It only works on text columns", "It is used to create indexes"],
    "correct_answer": "It performs calculations across a set of rows related to the current row, but WITHOUT collapsing them into a single output row (preserving the original row identity)",
    "explanation": "A `GROUP BY` collapses 100 rows into 1 row. A Window Function (like `SUM() OVER(PARTITION BY department)`) calculates the department total but attaches it to ALL 100 individual employee rows, allowing you to compare each employee to their department average in a single query."
  },
  {
    "id": 82,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "RANK vs DENSE_RANK",
    "question": "If three employees tie for 1st place in salary, what is the difference in the output between `RANK()` and `DENSE_RANK()`?",
    "code_snippet": null,
    "options": ["There is no difference", "`RANK()` will assign 1, 1, 1, 4 (skipping 2 and 3); `DENSE_RANK()` will assign 1, 1, 1, 2 (no gaps in the ranking sequence)", "`DENSE_RANK()` skips numbers; `RANK()` does not", "`RANK()` only works on numbers"],
    "correct_answer": "`RANK()` will assign 1, 1, 1, 4 (skipping 2 and 3); `DENSE_RANK()` will assign 1, 1, 1, 2 (no gaps in the ranking sequence)",
    "explanation": "Use `RANK()` for standard competitions (if two people tie for 1st, the next person is 3rd). Use `DENSE_RANK()` when you need contiguous ranking tiers (like assigning users to top 10%, top 20% buckets)."
  },
  {
    "id": 83,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Common Table Expressions (CTEs)",
    "question": "What is the primary benefit of using a CTE (the `WITH` clause) in a complex SQL query?",
    "code_snippet": null,
    "options": ["It makes the query run exponentially faster", "It allows you to define a temporary, named result set that can be referenced multiple times in the main query, vastly improving readability and maintainability", "It permanently saves the data to a new table", "It encrypts the query results"],
    "correct_answer": "It allows you to define a temporary, named result set that can be referenced multiple times in the main query, vastly improving readability and maintainability",
    "explanation": "Instead of writing massive, nested subqueries that are impossible to read, CTEs let you break complex logic into named, logical steps (e.g., `WITH active_users AS (...) SELECT * FROM active_users`). The database optimizer treats them similarly to inline views."
  },
  {
    "id": 84,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Recursive CTEs",
    "question": "What is a 'Recursive CTE' used for in PostgreSQL?",
    "code_snippet": null,
    "options": ["Running the same query 10 times", "Querying hierarchical or tree-structured data, such as organizational charts, bill-of-materials, or comment threads with nested replies", "Recursively deleting tables", "Backing up the database"],
    "correct_answer": "Querying hierarchical or tree-structured data, such as organizational charts, bill-of-materials, or comment threads with nested replies",
    "explanation": "A Recursive CTE consists of a base case (the root node) and a recursive member that joins back to the CTE itself. It allows you to traverse infinite-depth trees in a single, elegant SQL query without needing to write complex application-level loops."
  },
  {
    "id": 85,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Materialized Views",
    "question": "How does a 'Materialized View' differ from a standard SQL View?",
    "code_snippet": null,
    "options": ["They are exactly the same", "A standard View is a saved query that runs every time it's accessed; a Materialized View physically stores the query result on disk, making reads instant but requiring manual refreshes", "Materialized Views are only for NoSQL", "Standard Views are encrypted"],
    "correct_answer": "A standard View is a saved query that runs every time it's accessed; a Materialized View physically stores the query result on disk, making reads instant but requiring manual refreshes",
    "explanation": "If you have a complex analytics query that takes 10 seconds to run, a standard View takes 10 seconds every time a user loads the dashboard. A Materialized View runs the 10-second query once, saves the result, and serves the dashboard in 5ms until you tell it to refresh."
  },
  {
    "id": 86,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Concurrent Materialized View Refresh",
    "question": "What is the benefit of using `REFRESH MATERIALIZED VIEW CONCURRENTLY` in PostgreSQL?",
    "code_snippet": null,
    "options": ["It refreshes the view faster", "It allows users to continue reading the OLD data from the materialized view while the database calculates and swaps in the NEW data in the background, preventing downtime", "It deletes the old data first", "It automatically creates indexes"],
    "correct_answer": "It allows users to continue reading the OLD data while the database calculates and swaps in the NEW data in the background, preventing downtime",
    "explanation": "A standard refresh locks the view, causing errors for users trying to read it during the refresh. `CONCURRENTLY` requires a `UNIQUE INDEX` on the materialized view, but it ensures zero-downtime updates for your analytics dashboards."
  },
  {
    "id": 87,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Redis (Cache-Aside Pattern)",
    "question": "What is the 'Cache-Aside' (or Lazy Loading) pattern in application architecture?",
    "code_snippet": null,
    "options": ["The database automatically pushes data to Redis", "The application checks Redis first; if the data is missing (cache miss), it queries the database, writes the result to Redis with a TTL, and then returns it to the user", "Redis replaces the database entirely", "The application writes to Redis and the database simultaneously on every request"],
    "correct_answer": "The application checks Redis first; if the data is missing (cache miss), it queries the database, writes the result to Redis with a TTL, and then returns it to the user",
    "explanation": "Cache-Aside is the most common caching strategy. It ensures that the cache only stores data that is actually requested, and the application logic remains in full control of the cache invalidation lifecycle."
  },
  {
    "id": 88,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "The Cache Stampede Problem",
    "question": "What is a 'Cache Stampede' (or Thundering Herd), and how do you prevent it?",
    "code_snippet": null,
    "options": ["When Redis runs out of memory", "When a highly requested cache key expires, causing thousands of concurrent requests to simultaneously miss the cache and hammer the database; prevented by using mutex locks or probabilistic early expiration", "When the network cable is unplugged", "When the database crashes"],
    "correct_answer": "When a highly requested cache key expires, causing thousands of concurrent requests to simultaneously miss the cache and hammer the database; prevented by using mutex locks or probabilistic early expiration",
    "explanation": "If a popular user profile expires from Redis, 5,000 requests might hit the DB at the exact same millisecond to rebuild it. You prevent this by using a distributed lock (only ONE request rebuilds the cache, others wait) or by refreshing the cache in the background *before* the TTL actually hits zero."
  },
  {
    "id": 89,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Redis Data Structures",
    "question": "Which Redis data structure is perfectly suited for building a real-time global leaderboard?",
    "code_snippet": null,
    "options": ["Strings", "Hashes", "Sorted Sets (ZSET)", "Lists"],
    "correct_answer": "Sorted Sets (ZSET)",
    "explanation": "A Sorted Set stores unique members associated with a floating-point 'score'. Redis automatically keeps them sorted by score, allowing you to fetch the 'Top 10 players' or a user's 'global rank' in O(log(N)) time with a single command (`ZREVRANGE`)."
  },
  {
    "id": 90,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Database Scaling (Read Replicas)",
    "question": "What is the primary purpose of a 'Read Replica' in a PostgreSQL architecture?",
    "code_snippet": null,
    "options": ["To handle all write operations", "To offload read-heavy traffic from the primary database by asynchronously copying data to secondary servers", "To backup the database to S3", "To encrypt the data"],
    "correct_answer": "To offload read-heavy traffic from the primary database by asynchronously copying data to secondary servers",
    "explanation": "If your app does 1,000 writes and 10,000 reads per second, the primary DB will choke. You route all `SELECT` queries to the Read Replicas, and all `INSERT/UPDATE` queries to the Primary. Note: Replicas have 'eventual consistency'—a read immediately after a write might return stale data."
  },
  {
    "id": 91,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Table Partitioning",
    "question": "What is 'Table Partitioning' in PostgreSQL, and when should you use it?",
    "code_snippet": null,
    "options": ["Splitting the database into multiple servers", "Dividing a single, massive logical table into smaller physical pieces (e.g., by month or region) to improve query performance and maintenance", "Encrypting tables", "Creating backup copies of tables"],
    "correct_answer": "Dividing a single, massive logical table into smaller physical pieces (e.g., by month or region) to improve query performance and maintenance",
    "explanation": "If you have a 500GB `logs` table, querying the last 7 days still scans the whole table. By partitioning by `created_at` (Range Partitioning), PostgreSQL uses 'Partition Pruning' to completely ignore the old partitions, making the query instant. It also allows you to drop old data instantly by just dropping the old partition."
  },
  {
    "id": 92,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Database Sharding",
    "question": "What is 'Sharding' in database architecture?",
    "code_snippet": null,
    "options": ["Compressing the database", "Horizontally splitting a database across multiple separate server instances, where each server (shard) holds a subset of the total data (e.g., Users A-M on Shard 1, N-Z on Shard 2)", "Creating read replicas", "Backing up the database"],
    "correct_answer": "Horizontally splitting a database across multiple separate server instances, where each server holds a subset of the total data",
    "explanation": "When a single PostgreSQL server can no longer handle the CPU/RAM requirements of your data (even with read replicas), you shard it. This introduces massive complexity (cross-shard joins are nearly impossible), so it is always the absolute last resort."
  },
  {
    "id": 93,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Prisma Interactive Transactions",
    "question": "How do you execute multiple dependent database queries in a single Prisma transaction?",
    "code_snippet": null,
    "options": ["Using Promise.all()", "Using the interactive transaction API: `prisma.$transaction(async (tx) => { await tx.user.update(); await tx.post.create(); })`", "Using a standard try/catch block", "Prisma does not support transactions"],
    "correct_answer": "Using the interactive transaction API: `prisma.$transaction(async (tx) => { ... })`",
    "explanation": "The interactive transaction passes a special `tx` (transaction) client into the callback. All queries executed on `tx` are queued and only committed to the database if the callback completes successfully. If an error is thrown, it automatically rolls back."
  },
  {
    "id": 94,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Drizzle Relational Queries",
    "question": "How does Drizzle ORM handle eager loading (fetching a user and all their posts in one go) without writing manual JOINs?",
    "code_snippet": null,
    "options": ["It doesn't support relations", "Using the `db.query.users.findMany({ with: { posts: true } })` API, which automatically generates the optimal SQL JOINs under the hood", "By making two separate queries", "By using GraphQL"],
    "correct_answer": "Using the `db.query.users.findMany({ with: { posts: true } })` API, which automatically generates the optimal SQL JOINs under the hood",
    "explanation": "Drizzle's relational query API provides a Prisma-like developer experience while maintaining Drizzle's core philosophy of generating highly optimized, readable SQL under the hood."
  },
  {
    "id": 95,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Serverless Database Challenges",
    "question": "Why do traditional PostgreSQL connection pools struggle in Serverless environments (like AWS Lambda or Vercel Functions)?",
    "code_snippet": null,
    "options": ["Serverless is too slow", "Serverless functions scale horizontally to thousands of concurrent instances, each opening its own DB connections, which quickly exhausts PostgreSQL's maximum connection limit (usually ~100)", "PostgreSQL doesn't support serverless", "Serverless functions don't have internet access"],
    "correct_answer": "Serverless functions scale horizontally to thousands of concurrent instances, each opening its own DB connections, which quickly exhausts PostgreSQL's maximum connection limit",
    "explanation": "If 1,000 Lambda instances spin up and each opens 3 connections, you need 3,000 DB connections. To fix this, you must use a centralized connection pooler (like PgBouncer) or a serverless database driver (like the Neon Serverless Driver) that multiplexes connections over HTTP/WebSockets."
  },
  {
    "id": 96,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Database Indexing in ORMs",
    "question": "How do you define a custom database index in a Prisma schema file?",
    "code_snippet": null,
    "options": ["You cannot define indexes in Prisma", "By using the `@@index([field1, field2])` attribute at the model level", "By writing raw SQL triggers", "By using the `@unique` attribute only"],
    "correct_answer": "By using the `@@index([field1, field2])` attribute at the model level",
    "explanation": "While `@unique` creates a unique index, `@@index` allows you to create standard B-Tree composite indexes directly from your Prisma schema, which are then generated in the SQL migration files."
  },
  {
    "id": 97,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Database Locking (Pessimistic vs Optimistic)",
    "question": "What is 'Optimistic Concurrency Control' (Optimistic Locking) in a database?",
    "code_snippet": null,
    "options": ["Locking the table before reading", "Assuming conflicts are rare; you read the data, modify it, and check a 'version' column before saving. If the version changed, you reject the write and force a retry", "Using the Serializable isolation level", "Disabling transactions"],
    "correct_answer": "Assuming conflicts are rare; you read the data, modify it, and check a 'version' column before saving. If the version changed, you reject the write and force a retry",
    "explanation": "Pessimistic locking (`SELECT ... FOR UPDATE`) locks the row and blocks other users. Optimistic locking doesn't lock anything; it just adds a `WHERE version = 1` to the update. If the update affects 0 rows, you know someone else beat you to it."
  },
  {
    "id": 98,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Row-Level Security (RLS)",
    "question": "What is 'Row-Level Security' (RLS) in PostgreSQL?",
    "code_snippet": null,
    "options": ["Encrypting specific rows", "A feature that allows you to define policies that restrict which rows a specific user/role can read or modify, directly enforced by the database engine", "Password protecting tables", "Hiding columns from the ORM"],
    "correct_answer": "A feature that allows you to define policies that restrict which rows a specific user/role can read or modify, directly enforced by the database engine",
    "explanation": "Instead of writing `WHERE tenant_id = 123` in every single Node.js query, you enable RLS and tell Postgres: 'If the user's role is 'tenant', automatically append `WHERE tenant_id = current_setting('app.current_tenant')` to ALL queries.' It makes multi-tenant apps incredibly secure."
  },
  {
    "id": 99,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Database Backups (WAL)",
    "question": "What are PostgreSQL 'WAL' (Write-Ahead Logs) used for in the context of backups?",
    "code_snippet": null,
    "options": ["Storing user passwords", "They record every change made to the database before it's written to the data file, enabling Point-In-Time Recovery (PITR) to restore the DB to any specific second", "Compressing the database", "Routing network traffic"],
    "correct_answer": "They record every change made to the database before it's written to the data file, enabling Point-In-Time Recovery (PITR)",
    "explanation": "If you take a full backup at midnight, and the server crashes at 2:00 PM, you lose 14 hours of data. But if you have the WAL logs, you can restore the midnight backup and 'replay' the WAL logs up to exactly 1:59:59 PM, losing almost zero data."
  },
  {
    "id": 100,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Module 2 Wrap-up (The Data Zen)",
    "question": "You have now mastered Advanced SQL, Redis, Scaling, and ORMs. What is the ultimate hallmark of a Backend Engineer who has conquered the data layer?",
    "code_snippet": null,
    "options": ["Memorizing every PostgreSQL configuration flag", "Understanding that data is the most critical, persistent asset in any system; and knowing exactly when to use a B-Tree index, when to introduce Redis, when to scale to read replicas, and how to guarantee ACID compliance without sacrificing performance", "Avoiding the use of databases", "Always using NoSQL"],
    "correct_answer": "Understanding that data is the most critical, persistent asset in any system; and knowing exactly when to use a B-Tree index, when to introduce Redis, when to scale to read replicas, and how to guarantee ACID compliance without sacrificing performance",
    "explanation": "Anyone can write a `SELECT * FROM users` query. A true Senior Backend Engineer understands the physical reality of the disk, the network, and the memory, and designs data architectures that remain fast, consistent, and unbreakable as the company scales from 100 to 100 million users."
  }
]
[
  {
    "id": 101,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "JSON vs JSONB",
    "question": "What is the fundamental difference between the `JSON` and `JSONB` data types in PostgreSQL?",
    "code_snippet": null,
    "options": ["They are exactly the same", "`JSON` stores an exact copy of the input text (fast to insert, slow to query); `JSONB` stores data in a decomposed binary format (slower to insert, but vastly faster to query and index)", "`JSONB` is only for arrays", "`JSON` is deprecated"],
    "correct_answer": "`JSON` stores an exact copy of the input text; `JSONB` stores data in a decomposed binary format",
    "explanation": "Always use `JSONB` unless you have a highly specific, rare need to preserve exact whitespace or duplicate keys. `JSONB` strips insignificant whitespace, deduplicates keys, and allows for indexing, making it the standard for semi-structured data."
  },
  {
    "id": 102,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "JSONB Operators (-> vs ->>)",
    "question": "What is the difference between the `->` and `->>` operators when querying JSONB?",
    "code_snippet": null,
    "options": ["There is no difference", "`->` extracts the value as a JSONB object; `->>` extracts the value as plain text", "`->>` is for arrays", "`->` is deprecated"],
    "correct_answer": "`->` extracts the value as a JSONB object; `->>` extracts the value as plain text",
    "explanation": "If you have `{'age': 30}`, `data->'age'` returns the JSONB value `"30"` (with quotes), while `data->>'age'` returns the text string `30` (without quotes). Use `->>` when you need to cast the result to an integer or compare it as text."
  },
  {
    "id": 103,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "code-fill-in-blank",
    "topic": "Updating JSONB (jsonb_set)",
    "question": "Fill in the blank to correctly update a nested key inside a JSONB column without overwriting the rest of the object.",
    "code_snippet": "UPDATE users \nSET metadata = _______(metadata, '{address, city}', '\"New York\"'::jsonb)\nWHERE id = 1;",
    "options": ["json_update", "jsonb_set", "json_replace", "set_json"],
    "correct_answer": "jsonb_set",
    "explanation": "`jsonb_set(target, path, new_value)` allows you to surgically update a specific nested path (like `{address, city}`) inside a JSONB document, returning a brand new JSONB object with the change applied."
  },
  {
    "id": 104,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "JSONB Indexing (jsonb_path_ops)",
    "question": "When creating a GIN index on a JSONB column, what is the advantage of using the `jsonb_path_ops` operator class?",
    "code_snippet": null,
    "options": ["It makes inserts faster", "It creates a significantly smaller and faster index, specifically optimized for the `@>` (contains) operator, though it doesn't support checking if a key exists", "It encrypts the JSON", "It automatically validates the JSON schema"],
    "correct_answer": "It creates a significantly smaller and faster index, specifically optimized for the `@>` (contains) operator",
    "explanation": "If you only ever query JSONB using the 'contains' operator (e.g., `WHERE metadata @> '{"role": "admin"}'`), `jsonb_path_ops` is vastly superior to the default GIN index in both size and speed."
  },
  {
    "id": 105,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Full-Text Search (tsvector & tsquery)",
    "question": "What are the core data types used for Full-Text Search (FTS) in PostgreSQL?",
    "code_snippet": null,
    "options": ["VARCHAR and TEXT", "`tsvector` (represents the document) and `tsquery` (represents the search query)", "JSONB and HSTORE", "BLOB and CLOB"],
    "correct_answer": "`tsvector` (represents the document) and `tsquery` (represents the search query)",
    "explanation": "`tsvector` converts text into a sorted list of normalized 'lexemes' (words stripped of stop-words like 'the' or 'a'). `tsquery` represents the boolean search logic. You match them together to perform blazing-fast text searches."
  },
  {
    "id": 106,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "code-fill-in-blank",
    "topic": "Full-Text Search (Matching)",
    "question": "Fill in the blank with the PostgreSQL operator used to check if a `tsvector` document matches a `tsquery` search term.",
    "code_snippet": "SELECT * FROM articles \nWHERE to_tsvector('english', content) _______ to_tsquery('english', 'database & performance');",
    "options": ["==", "LIKE", "@@", "IN"],
    "correct_answer": "@@",
    "explanation": "The `@@` operator is the match operator for Full-Text Search. It returns true if the `tsvector` (the document) satisfies the `tsquery` (the search terms). It is highly optimized and can be backed by a GIN index."
  },
  {
    "id": 107,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Timezones (TIMESTAMP vs TIMESTAMPTZ)",
    "question": "Why is it considered a best practice to almost always use `TIMESTAMPTZ` (Timestamp with Time Zone) instead of `TIMESTAMP` in PostgreSQL?",
    "code_snippet": null,
    "options": ["It uses less disk space", "`TIMESTAMP` ignores timezones and stores the exact wall-clock time written, which causes massive bugs across global apps; `TIMESTAMPTZ` converts the input to UTC and stores an absolute point in time", "`TIMESTAMPTZ` is required by Prisma", "`TIMESTAMP` is deprecated"],
    "correct_answer": "`TIMESTAMP` ignores timezones and stores the exact wall-clock time; `TIMESTAMPTZ` converts to UTC and stores an absolute point in time",
    "explanation": "If you store `TIMESTAMP '2023-01-01 12:00:00'`, the DB doesn't know if that's New York noon or Tokyo noon. `TIMESTAMPTZ` normalizes everything to UTC internally, ensuring that 'now' is universally consistent regardless of where the server or user is located."
  },
  {
    "id": 108,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "How TIMESTAMPTZ is Stored",
    "question": "How does PostgreSQL physically store a `TIMESTAMPTZ` value in the database file?",
    "code_snippet": null,
    "options": ["It stores the original string and the timezone offset", "It converts the value to UTC and stores ONLY the UTC timestamp; the timezone is only applied when retrieving the data based on the session's timezone setting", "It stores it as a Unix timestamp string", "It stores it in the server's local timezone"],
    "correct_answer": "It converts the value to UTC and stores ONLY the UTC timestamp",
    "explanation": "PostgreSQL does NOT store the original timezone offset. It converts the input to UTC (epoch time) and saves that. When you `SELECT` it, Postgres converts it back to the timezone of the current database connection. This is why you should always configure your Node.js DB connection to use UTC."
  },
  {
    "id": 109,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Node.js Date Handling Strategy",
    "question": "What is the golden rule for handling dates and timezones in a full-stack TypeScript application?",
    "code_snippet": null,
    "options": ["Store local time in the DB, format in the DB", "Store everything in UTC in the database, transmit as ISO-8601 UTC strings over the API, and format to the user's local timezone ONLY on the client-side UI", "Store Unix timestamps as integers", "Use the browser's Date object in the backend"],
    "correct_answer": "Store everything in UTC in the database, transmit as ISO-8601 UTC strings over the API, and format to the user's local timezone ONLY on the client-side UI",
    "explanation": "By keeping the backend entirely in UTC, you eliminate timezone math from your server logic. The frontend (using libraries like `date-fns` or `dayjs`) takes the UTC string and converts it to the user's local timezone for display."
  },
  {
    "id": 110,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Soft Deletes",
    "question": "What is a 'Soft Delete', and what is its primary advantage?",
    "code_snippet": null,
    "options": ["Using `DELETE` with a `LIMIT` clause", "Instead of executing a `DELETE` statement, you set a `deleted_at` timestamp column; this preserves data for auditing, prevents breaking foreign key constraints, and allows for 'undo' functionality", "Compressing the deleted rows", "Moving deleted rows to a different database"],
    "correct_answer": "Instead of executing a `DELETE` statement, you set a `deleted_at` timestamp column",
    "explanation": "Soft deletes prevent accidental data loss and maintain referential integrity (e.g., if a User is hard-deleted, their Orders might violate a foreign key constraint). The trade-off is that you must remember to add `WHERE deleted_at IS NULL` to all your queries (or use a DB view/ORM middleware to handle it)."
  },
  {
    "id": 111,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Database Triggers",
    "question": "What is a 'Database Trigger', and what is a common, safe use case for it?",
    "code_snippet": null,
    "options": ["A function that runs when the server starts", "A function automatically executed by the DB in response to certain events (INSERT/UPDATE/DELETE); a safe use case is maintaining an immutable audit log or updating a `updated_at` timestamp", "A tool to backup the database", "A way to encrypt passwords"],
    "correct_answer": "A function automatically executed by the DB in response to certain events; a safe use case is maintaining an immutable audit log",
    "explanation": "Triggers are powerful but dangerous if used for core business logic (they hide logic from the application code). They are best reserved for structural guarantees like audit trails, full-text search vector updates, or soft-delete cascading."
  },
  {
    "id": 112,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "SQL Injection Prevention",
    "question": "How do parameterized queries (or prepared statements) prevent SQL Injection attacks?",
    "code_snippet": null,
    "options": ["By encrypting the SQL string", "By sending the SQL query structure and the user data to the database separately, ensuring the DB treats the user data strictly as literal values, never as executable SQL commands", "By removing all punctuation from user input", "By using a Web Application Firewall"],
    "correct_answer": "By sending the SQL query structure and the user data to the database separately",
    "explanation": "If you use string concatenation (`SELECT * FROM users WHERE name = '${input}'`), an input like `'; DROP TABLE users; --` will execute. Parameterized queries (`SELECT * FROM users WHERE name = $1`) force the DB to parse the SQL first, treating `$1` purely as a string literal."
  },
  {
    "id": 113,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "OLTP vs OLAP",
    "question": "What is the difference between OLTP and OLAP databases?",
    "code_snippet": null,
    "options": ["OLTP is for NoSQL; OLAP is for SQL", "OLTP (Online Transaction Processing) is optimized for fast, atomic, row-level inserts/updates (like PostgreSQL); OLAP (Online Analytical Processing) is optimized for complex, read-heavy queries over massive datasets (like Snowflake)", "OLTP is cloud-based; OLAP is on-premise", "They are the same thing"],
    "correct_answer": "OLTP is optimized for fast, atomic transactions; OLAP is optimized for complex, read-heavy analytical queries",
    "explanation": "Your main PostgreSQL database is OLTP. It handles user logins, cart updates, and payments. When you need to run a query like 'Calculate the total revenue per country for the last 5 years', you offload that to an OLAP data warehouse so you don't crash your production app."
  },
  {
    "id": 114,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Columnar Databases",
    "question": "Why are Columnar Databases (like ClickHouse, Snowflake, or BigQuery) vastly faster than row-based databases for analytical queries?",
    "code_snippet": null,
    "options": ["They use faster network cables", "They store data by column rather than by row, meaning a query that only needs to sum one specific column only reads that specific column from disk, ignoring the rest of the table", "They don't use indexes", "They compress data using ZIP"],
    "correct_answer": "They store data by column rather than by row, meaning a query only reads the specific columns it needs from disk",
    "explanation": "In a row-based DB (Postgres), to sum the `price` column, the DB must read every single byte of every row (including `name`, `email`, `address`). In a columnar DB, the `price` column is stored contiguously on disk, allowing the DB to scan only the exact data needed, resulting in 100x faster analytics."
  },
  {
    "id": 115,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Star Schema",
    "question": "In a Data Warehouse 'Star Schema', what is the difference between a 'Fact Table' and a 'Dimension Table'?",
    "code_snippet": null,
    "options": ["Fact tables store text; Dimension tables store numbers", "Fact tables contain the measurable, quantitative business events (e.g., sales amount, quantity); Dimension tables contain the descriptive context (e.g., user name, product category, date)", "Fact tables are NoSQL; Dimension tables are SQL", "There is no difference"],
    "correct_answer": "Fact tables contain the measurable events; Dimension tables contain the descriptive context",
    "explanation": "The Fact table is the center of the star, holding millions of rows of transactions. The Dimension tables branch out, holding the 'who, what, where, and when' that give context to the facts."
  },
  {
    "id": 116,
    "level": "intermediate",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "ETL vs ELT",
    "question": "What is the modern shift from ETL to ELT in data engineering?",
    "code_snippet": null,
    "options": ["ETL is for SQL; ELT is for NoSQL", "In ETL, data is transformed before loading; in modern ELT (Extract, Load, Transform), raw data is loaded directly into the warehouse first, and transformed using the warehouse's own massive compute power", "ELT is slower", "ETL is deprecated"],
    "correct_answer": "In modern ELT, raw data is loaded directly into the warehouse first, and transformed using the warehouse's compute power",
    "explanation": "Because modern cloud data warehouses (Snowflake, BigQuery) have virtually infinite compute, it's faster to just dump the raw JSON/CSV data in first (Load), and then use SQL to transform it (Transform) rather than bottlenecking through a separate ETL server."
  },
  {
    "id": 117,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Safe Migrations (Expand & Contract)",
    "question": "What is the 'Expand and Contract' (or Parallel Change) pattern for deploying database migrations without downtime?",
    "code_snippet": null,
    "options": ["Shutting down the server, running the migration, and restarting", "1. Expand: Add the new column/table (backward compatible). 2. Migrate: Update app code to write to both old and new. 3. Backfill: Copy old data to new. 4. Contract: Remove old column and old code in a later release", "Running the migration on a Friday night", "Using a single massive SQL script"],
    "correct_answer": "1. Expand: Add the new column. 2. Migrate: Update app to write to both. 3. Backfill data. 4. Contract: Remove old column later",
    "explanation": "If you rename a column in a single migration, the old app code will crash, and the new app code will fail on the old DB. Expand/Contract ensures that at every single step, both the old code and the new code can function perfectly, enabling zero-downtime deployments."
  },
  {
    "id": 118,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Backfilling Data Safely",
    "question": "When backfilling a new column for 10 million existing rows, what is the safest approach to avoid locking the database or crashing the CPU?",
    "code_snippet": null,
    "options": ["`UPDATE users SET new_col = 'value';`", "Write a script that updates the rows in small batches (e.g., 1,000 at a time) with a slight delay between batches, running during off-peak hours", "Delete the table and recreate it", "Use a `SELECT` statement instead"],
    "correct_answer": "Write a script that updates the rows in small batches with a slight delay between batches",
    "explanation": "A massive `UPDATE` will lock the table, consume all I/O, and cause replication lag. Batched updates (often using a script that targets rows by primary key ranges) keep the database responsive for live user traffic."
  },
  {
    "id": 119,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Security Definer Views",
    "question": "What is a 'Security Definer' view or function in PostgreSQL?",
    "code_snippet": null,
    "options": ["A view that requires a password", "A view or function that executes with the privileges of the user who CREATED it, rather than the user who is CALLING it, allowing you to grant access to specific data without giving full table access", "A view that is encrypted", "A view that only works for admins"],
    "correct_answer": "A view that executes with the privileges of the user who CREATED it",
    "explanation": "This is incredibly useful for exposing specific, aggregated data to a less-privileged application role. The app role can query the view to get the data, but cannot directly query the underlying sensitive tables."
  },
  {
    "id": 120,
    "level": "advanced",
    "module_id": "node_mod_02_databases",
    "module_name": "Database Mastery & Advanced SQL",
    "type": "conceptual",
    "topic": "Module 2 Wrap-up (The Data Zen)",
    "question": "You have now completed the entire 60-question deep dive into Database Mastery. What is the ultimate hallmark of a Backend Engineer who has conquered the data layer?",
    "code_snippet": null,
    "options": ["Memorizing every SQL command", "Understanding that data is the most critical, persistent asset in any system; and knowing exactly when to use a B-Tree index, when to introduce Redis, when to scale to read replicas, and how to guarantee ACID compliance without sacrificing performance", "Avoiding the use of databases", "Always using NoSQL"],
    "correct_answer": "Understanding that data is the most critical, persistent asset; and knowing exactly when to use a B-Tree index, when to introduce Redis, when to scale to read replicas, and how to guarantee ACID compliance without sacrificing performance",
    "explanation": "Anyone can write a `SELECT * FROM users` query. A true Senior Backend Engineer understands the physical reality of the disk, the network, and the memory, and designs data architectures that remain fast, consistent, and unbreakable as the company scales from 100 to 100 million users."
  }
]
